// Mengekspor objek konfigurasi PostCSS secara default
export default {
  // Menentukan daftar plugin PostCSS yang diaktifkan
  plugins: {
    // Mengaktifkan plugin TailwindCSS untuk memproses utilitas CSS
    tailwindcss: {},
    // Mengaktifkan autoprefixer untuk menyisipkan vendor prefix CSS browser secara otomatis
    autoprefixer: {},
  },
}
